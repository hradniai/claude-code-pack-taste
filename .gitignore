# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Backups created by cc-config or manual edits
*.bak
*.bak-*
*.backup

# Local-only env files (never commit credentials)
.env
.env.local
.env.*.local

# OS / shell artifacts
.zsh_history
.bash_history

# Internal project memory (NOT for public). Only the root-level ones —
# `kernel/CLAUDE.md` is a legitimate part of the pack.
/CLAUDE.md
/WORKSTATE.md
